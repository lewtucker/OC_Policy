{
  "name": "@oc-policy/enforcement-plugin",
  "version": "1.0.0",
  "description": "OC Policy enforcement plugin — intercepts tool calls via before_tool_call hook",
  "type": "module",
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
